import { IView } from '@features/visor/core/interfaces/map-stuff/chronos-map-view.interfaz';
import { View } from 'ol';

export class ChronosMapView extends View {
	constructor(props: IView) {
		super(props);
	}
}